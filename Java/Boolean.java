public class Boolean {
    public static void main(String[] args) {
        int first = 2;
        int second = 5;
        boolean num = first < second;
        if (num) {
            System.out.println(first + " is less than " + second + "!");
        }

    }
}